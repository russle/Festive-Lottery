{
    "name": "festive-lottery-api",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:init": "wrangler d1 execute festive-lottery-db --file=./schema.sql",
        "db:init:local": "wrangler d1 execute festive-lottery-db --local --file=./schema.sql"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240117.0",
        "typescript": "^5.3.3",
        "wrangler": "^3.24.0"
    }
}